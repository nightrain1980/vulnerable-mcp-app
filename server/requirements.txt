# Vulnerable MCP Server Dependencies
# Pinned to versions with known CVEs for educational purposes

# Core framework
fastapi==0.63.0
uvicorn==0.13.4
pydantic==1.8.1

# HTTP & Networking (vulnerable versions)
requests==2.25.1
urllib3==1.26.4
httpx==0.16.0

# Cryptography & Auth (vulnerable versions)
cryptography==3.3.2
PyJWT==2.0.1
paramiko==2.7.2

# Data processing (vulnerable versions)
PyYAML==5.4
Pillow==9.0.0

# Task queue (vulnerable version)
celery==4.4.7

# Utilities
python-multipart==0.0.5
python-dotenv==0.19.0
